# Шаблон конфигурации для псевдорепозитория
{
    'type'	=> 'pseudo', # тип вызова. Варианты:	system (или sys),
			     #				repository (или rep),
			     #			 	pseudo

# параметры подключения к серверу системы RO
    'server'	=> {	'host'	=> 'somehost.somewhere',# хост сервера системы
			'port'	=> '8279',# использующийся порт сервера
			'proto' => 'http' # использующийся протокол
    },

# секция лог-файла
    'log'	=> {	'use_log'	=> 1,# флаг записи в лог
			'log_file'	=> '/var/log/ro_log' # местоположение
							     # лога
    },

    'signature' => '12345678901234567890123456789012',# уникальная подпись узла

# местоположение файлов с описанием псевдорепозитория
#    'source' => 'packages.xml' # в случае, если файл с описанием один
    'source' => ['packages.xml', 'packages2.xml'] # в случае, если файлов
						  # несколько
};
